Static assets — images, CSS, and logos used in the Streamlit UI.

Add here:
  - Team logo or FCA logo (PNG/SVG)
  - Any custom CSS overrides
  - Screenshot mockups used in the pitch deck

Streamlit loads static files from this folder when referenced as:
  st.image("frontend/static/your_image.png")
