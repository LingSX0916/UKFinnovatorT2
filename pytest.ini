[pytest]
# Put the repo root on sys.path so `import backend...` works from anywhere.
pythonpath = .
# CI runs the offline KYB engine suite (no API keys, no network):
#   pytest tests/kyb
addopts = -q
