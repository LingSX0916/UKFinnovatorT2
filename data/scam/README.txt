Red-light adverts — egregious breaches and scam promotions.

These are the showstoppers. Should return RED with multiple FAILs.

Files:
  coinvault_pro_TEST.txt     — synthetic RED showstopper (replace with real data when ready). Fires R1 (unauthorised), R2 (guaranteed
                          returns, "zero risk", "300%"), R6 (no prescribed crypto warning),
                          R7 (undisclosed celebrity endorsement), R8 (false "FCA approved").
                          CoinVault Pro also matches warning_list.json so the red alert badge
                          fires. Run this one live on stage.
                          Should return RED.

  coinvault_pro.png     — image version for testing the vision/screenshot scanning path
                          (add file here when ready)

  finfluencer_post_TEST.txt  — synthetic finfluencer post (replace with real data when ready). Tests R1 (no authorised
                          firm), R3 (FOMO), R4 (no risk warning), R7 (undisclosed paid promo).
                          Should return RED.
